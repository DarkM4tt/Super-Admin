/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */
const PartnerInfo = ({ orgId, setActiveComponent, setSelectedOrg }) => {
  return <div onClick={() => setActiveComponent("Partners")}>PartnerInfo</div>;
};

export default PartnerInfo;
